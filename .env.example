# Site (static export — no database required locally)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_NAME="ProVizient"

# Google Analytics (optional)
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# --- Azure Static Web Apps (set in Azure Portal → Configuration) ---
# These are used by Azure Functions in /api — not needed for local static preview.
# RESEND_API_KEY="re_xxxxxxxxxxxx"
# RESEND_FROM_EMAIL="ProVizient <noreply@provizient.com>"
# NOTIFICATION_EMAIL="contact@provizient.com"
