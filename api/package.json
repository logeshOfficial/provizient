{
  "name": "provizient-api",
  "version": "1.0.0",
  "private": true,
  "description": "ProVizient API — SMTP email via Nodemailer",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "nodemailer": "9.0.3"
  },
  "devDependencies": {
    "fast-check": "3.22.0",
    "jest": "29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  }
}
